import React from 'react';
import './Slider.css';
import { SliderProps } from './Slider.types';

export const Slider: React.FC<SliderProps> = (props) => {
  return <div className="placeholder">Slider Component</div>;
};

export default Slider;
