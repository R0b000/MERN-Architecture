import React from 'react';
import './Toaster.css';
import { ToasterProps } from './Toaster.types';

export const Toaster: React.FC<ToasterProps> = (props) => {
  return <div className="placeholder">Toaster Component</div>;
};

export default Toaster;
