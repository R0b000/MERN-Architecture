import React from 'react';
import './Tour.css';
import { TourProps } from './Tour.types';

export const Tour: React.FC<TourProps> = (props) => {
  return <div className="placeholder">Tour Component</div>;
};

export default Tour;
