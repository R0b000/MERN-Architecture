import React from 'react';
import './Timeline.css';
import { TimelineProps } from './Timeline.types';

export const Timeline: React.FC<TimelineProps> = (props) => {
  return <div className="placeholder">Timeline Component</div>;
};

export default Timeline;
