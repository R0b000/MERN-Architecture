import React from 'react';
import './Tabs.css';
import { TabsProps } from './Tabs.types';

export const Tabs: React.FC<TabsProps> = (props) => {
  return <div className="placeholder">Tabs Component</div>;
};

export default Tabs;
